name: Repository Dispatch
on:
  repository_dispatch:
    types: [workflow_dispatch]
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Dump the client payload context
        env:
          ENV_CONTEXT: ${{ toJson(github.event.env_params) }}
        run: echo "$ENV_CONTEXT"
        